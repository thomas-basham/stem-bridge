# StemBridge App Icons

Electron Forge is configured to use these files when they exist:

- `stembridge.icns` for macOS packaging
- `stembridge.ico` for Windows/Squirrel packaging
- `stembridge.png` for Linux DEB/RPM packaging

The app currently builds without custom icons. Add the platform icon files above before a branded release build.
